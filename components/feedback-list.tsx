import React from 'react'

const FeedbackList = ({ initialPosts, userId }: { initialPosts: any[], userId: string | null }) => {
    return (
        <div>FeedbackList</div>
    )
}

export default FeedbackList